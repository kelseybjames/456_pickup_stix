<h1><%= @artist.name %></h1>
<% @artist.songs.each do |song| %>
  <h4><%= song.name %></h4>
  <%= form_tag playlists_path do %>
    <%= select_tag(:playlist_id, options_for_select(@playlist_options)) %>
    <%= submit_tag 'Add to Playlist' %>
  <% end %>
<% end %>